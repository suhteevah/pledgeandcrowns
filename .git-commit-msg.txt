refactor(assets): migrate legacy 3 sprites to sprites/{category}/{name}_{frame}.png

Per CLAUDE.md's filename convention and resolving the dual-layout drift
art-lead flagged in batch 1: the legacy three sprites
(player/ferris/borrow_checker) sat flat at sprites/X.png while the
NPC batch shipped under sprites/npc/<slug>_idle_0.png. Now everything
follows the documented convention.

Player sprite gets its own category dir (sprites/player/) so future
protagonist animation frames have a home and we don't have to migrate
again when walk/idle/attack frames land. Ferris and Borrow Checker
join the NPC dir alongside the new batch.

Files renamed via git mv (history preserved):
  sprites/player.png         -> sprites/player/player_idle_0.png
  sprites/ferris.png         -> sprites/npc/ferris_idle_0.png
  sprites/borrow_checker.png -> sprites/npc/borrow_checker_idle_0.png

Updates:
- game/src/assets.rs: SPRITE_PLAYER, SPRITE_FERRIS, SPRITE_BORROW_CHECKER constants
- design/04b-art-deliverables.md: REF 01/02/03 manifest rows

Audit harness verified: every_sprite_path_resolves_to_an_existing_file
catches missed renames; CI green.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
